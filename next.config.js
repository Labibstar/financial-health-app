/** @type {import('next').NextConfig} */
const nextConfig = {}

module.exports = {
    distDir: 'build',
    pages: {
        exclude: ['/pages/_not-found'],
      },     
  }
  

module.exports = nextConfig
